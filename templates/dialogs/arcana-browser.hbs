{{!-- The Arcana browser is the shared catalogue shell with nothing added: source tabs (it
      has only one list, so none render), search, chip groups, rows, empty state. What makes
      it the ARCANA browser is the rows and facets ArcanaBrowserDialog feeds it. --}}
{{> "stonetop.catalog-shell"}}
